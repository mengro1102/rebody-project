# ReBody — 비용 구조 (2026-07 기준)

## 필수 고정비

| 항목 | 금액 | 비고 |
|---|---|---|
| **Google Play 개발자 등록** | **$25 (1회)** | 유일한 필수 지출. 계정당 1회 |
| 그 외 전부 | $0 | 무료 티어로 MVP 커버 |

## 무료 티어로 커버되는 항목

| 서비스 | 용도 | 무료 한도 | 주의 |
|---|---|---|---|
| Supabase | DB / Auth / Edge Functions / Storage | Free tier | **7일 미접속 시 프로젝트 일시정지** → `keepalive-ping` cron 필수 |
| Gemini API | 음식 인식 · 스케줄 파싱 · 주간 피드백 | 무료 티어(레이트리밋 有) | 유료 전환 시에도 월 $1~3 예상 |
| 공공데이터포털(MFDS) | 한식 영양성분 | 완전 무료 | 일일 호출 한도 有 → 캐시로 대응 |
| FCM | 푸시 | 발송량 무관 무료 | — |
| EAS Build | Android 빌드 | 플랜별 상이 | **착수 시점 대시보드에서 재확인**. OTA 우선 전략 유지 |
| RevenueCat | 인앱결제 | 월 추적매출 $2,500 이하 | MVP 초기엔 사실상 전액 무료 |
| Firebase Analytics / Crashlytics | 분석 · 크래시 | 무료 | — |
| GitHub Pages | 개인정보처리방침 호스팅 | 무료 | **공개 저장소 필요** |
| Figma | 아이콘 · 스토어 이미지 | 무료 플랜 | — |
| 도메인 | 브랜딩 | 생략 가능 | 트랙션 생긴 뒤 구매 |

---

## Gemini 비용 실측 추정

단가 (2026-07, per 1M tokens):

| 모델 | 입력 | 출력 |
|---|---|---|
| **gemini-3.1-flash-lite** (기본값) | $0.125 | $0.750 |
| gemini-2.5-flash-lite | $0.10 | $0.40 |
| gemini-3.5-flash-lite (승급용) | $0.30 | $2.50 |

**출력 단가가 입력의 6배** → 비용 통제의 핵심은 출력 토큰입니다.

### 호출별 토큰 예산 (코드에 하드 상한으로 반영됨)

| 호출 | 입력 | 출력 상한 | 건당 비용 |
|---|---|---|---|
| 음식 인식 (이미지 1장) | ~1,300 tok | **256** | ≈ $0.00035 |
| 스케줄 NL 파싱 | ~800 tok | **512** | ≈ $0.00048 |
| 주간 피드백 (집계값만) | ~600 tok | **700** | ≈ $0.00060 |

### DAU 100 시나리오 (월)

```
음식 스캔:  100명 × 3회 × 30일 = 9,000건
            캐시 히트율 50% 가정 → Gemini 실호출 4,500건
            4,500 × $0.00035 ≈ $1.58

스케줄 파싱: 신규 유입 300명 × 1.5회 = 450건
            450 × $0.00048 ≈ $0.22

주간 피드백: 100명 × 4주 = 400건
            400 × $0.00060 ≈ $0.24

────────────────────────────────
월 합계 ≈ $2.04   (무료 티어 내면 $0)
```

---

## 비용 최소화 장치 (코드에 구현된 것)

1. **영양정보 캐시** — `food_nutrition_cache`. 동일 음식 재스캔 시 MFDS/Gemini 호출 생략. 히트율이 곧 비용 절감률.
2. **서버측 스캔 쿼터** — `consume_scan_quota()`. 무료 3회/일을 DB에서 원자적으로 강제. 클라이언트 우회 불가.
3. **출력 토큰 상한 + structured output** — 모든 호출에 `maxOutputTokens` + `responseSchema`. 장문 서술 차단.
4. **집계 후 전송** — 주간 피드백은 원본 로그가 아니라 집계 통계만 전송. 토큰·프라이버시 동시 절감.
5. **모델 ID 환경변수화** — `GEMINI_MODEL`. 단가 변동/모델 종료 시 재배포 없이 교체.
6. **OTA 우선 배포** — JS/UI 변경은 `eas update`, 네이티브 변경 시에만 `eas build`.
7. **keepalive cron** — Supabase 무료 프로젝트 일시정지 방지 (정지되면 복구에 시간 소모 = 간접 비용).

---

## 배포 단계별 실제 지출 (2026-07-28 추가)

`06_MY_TASKS.md`의 배포 트랙 R0~R5 기준입니다.

| 단계 | 지출 | 근거 |
|---|---|---|
| R0 계정 개설 | **$25** | Play 개발자 등록 1회. 그 외 전부 무료 |
| R1 1인 검증 | $0 | EAS 무료 빌드 쿼터 1회 소비 |
| R2 내부 테스트 | $0 | 내부 트랙은 심사·비용 없음. RevenueCat도 무료 구간 |
| R3 화이트리스트 12명 × 14일 | **$0.2 미만** | 아래 계산 |
| R4 프로덕션 승인 | $0 | 심사 무료 |
| R5 단계 출시 | $0 | DAU가 붙은 뒤에야 Gemini 비용 발생 |

```
R3 구간 Gemini 상한 (최악의 경우 = 전원이 매일 무료 쿼터를 소진)
  12명 × 3스캔 × 14일 = 504건
  캐시 히트 0% 가정해도 504 × $0.00035 ≈ $0.18
```

**결론: 테스트 전 구간이 사실상 무료입니다.** 배포를 단계로 나누는 이유는 비용 절감이 아니라
① Google의 12명×14일 규칙이 강제하는 달력 시간, ② 되돌릴 수 없는 결정의 분리,
③ 테스터 이탈 방지(= 14일 카운터 리셋 방지)입니다.

### 유일한 실질 자원 제약: EAS 빌드 쿼터

돈이 아니라 **빌드 횟수**가 이 프로젝트의 병목입니다. 대응:

- `eas build`는 **각 R 단계 진입 시 1회씩**만 (R1 development / R2 preview / R5 production)
- JS·UI 변경은 전부 `eas update` (OTA)
- **R3 진입 후 네이티브 변경 금지** — 재빌드는 테스터 재설치를 유발하고, 이탈 1명이 14일을 리셋시킵니다

---

## 결론

원문 착수 문서의 결론은 유효합니다. **Play에 MVP를 올리는 데 필수로 나가는 돈은 개발자 등록비 $25 한 번뿐**이고, 트래픽이 붙어도 Gemini 비용은 월 몇 달러 수준입니다. 실질 제약은 돈이 아니라 시간입니다.
